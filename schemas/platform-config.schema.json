{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/msckite/az-platform-kite/refs/heads/main/schemas/platform-config.schema.json",
  "schemaVersion": "1.0.0",
  "title": "Platform Config Schema",
  "description": "Per-service configuration describing the Azure and GitHub environments to provision for a repository.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "templateVersion",
    "resourceGroups",
    "securityGroups",
    "environments"
  ],
  "properties": {
    "$schema": {
      "description": "Optional schema reference for tooling.",
      "type": "string"
    },
    "templateVersion": {
      "description": "Version of the template contract.",
      "const": "1.0.0"
    },
    "resourceGroups": {
      "description": "Azure resource groups to create first, ahead of any security group, environment, identity, or GitHub environment resources. Each represents a stage in the deployment (e.g. dev, stg, prd).",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/resourceGroup"
      }
    },
    "securityGroups": {
      "description": "Microsoft Entra security groups to create once RBAC roles are assigned against the resource groups created above. Created as phase 2, ahead of any per-environment identity or GitHub environment resources.",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/securityGroup"
      }
    },
    "environments": {
      "description": "The environments (e.g. dev, prd) to provision a federated user-assigned identity and a GitHub environment for. Each environment references a resource group created in the top-level 'resourceGroups' array.",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/environmentConfig"
      }
    }
  },
  "definitions": {
    "environmentConfig": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "displayName",
        "environmentCode",
        "resourceGroupId",
        "userAssignedIdentity",
        "githubEnvironment"
      ],
      "properties": {
        "displayName": {
          "description": "Human-readable name for the environment (e.g. Development).",
          "type": "string",
          "minLength": 1
        },
        "environmentCode": {
          "description": "Short lowercase code for the environment, used in resource names, the GitHub environment name, and the OIDC subject claim.",
          "type": "string",
          "pattern": "^[a-z0-9]+$",
          "examples": [
            "dev",
            "prd"
          ]
        },
        "description": {
          "description": "Optional description of the environment.",
          "type": "string"
        },
        "resourceGroupId": {
          "description": "Id of the resource group (from the top-level 'resourceGroups' array) this environment deploys into.",
          "type": "string",
          "minLength": 1
        },
        "userAssignedIdentity": {
          "$ref": "#/definitions/userAssignedIdentity"
        },
        "githubEnvironment": {
          "$ref": "#/definitions/githubEnvironment"
        }
      }
    },
    "resourceGroup": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "name",
        "location"
      ],
      "properties": {
        "id": {
          "description": "Local id used to reference this resource group from 'environmentConfig.resourceGroupId' and 'roleAssignment.resourceGroupId'. Not an Azure identifier.",
          "type": "string",
          "minLength": 1
        },
        "name": {
          "description": "Azure resource group name.",
          "type": "string",
          "minLength": 1
        },
        "location": {
          "description": "Azure location name for the resource group.",
          "type": "string",
          "minLength": 1
        },
        "tags": {
          "description": "Tags to apply to the resource group.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      }
    },
    "securityGroup": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "displayName",
        "mailNickName",
        "roleAssignments"
      ],
      "properties": {
        "displayName": {
          "description": "Display name of the Microsoft Entra security group.",
          "type": "string",
          "minLength": 1
        },
        "mailNickName": {
          "description": "Mail nickname (alias) of the security group.",
          "type": "string",
          "minLength": 1
        },
        "description": {
          "description": "Optional description of the security group.",
          "type": "string"
        },
        "roleAssignments": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/definitions/roleAssignment"
          }
        }
      }
    },
    "userAssignedIdentity": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "federatedCredential",
        "roleAssignments"
      ],
      "properties": {
        "name": {
          "description": "Name of the user-assigned managed identity.",
          "type": "string",
          "minLength": 1
        },
        "federatedCredential": {
          "$ref": "#/definitions/federatedCredential"
        },
        "roleAssignments": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/definitions/roleAssignment"
          }
        }
      }
    },
    "federatedCredential": {
      "description": "Federated identity credential (OIDC trust) configured on the user-assigned managed identity. The subject claim is not stored here; it is computed by the deployment command from the live GitHub repository ID.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "issuer",
        "subjectType",
        "audiences"
      ],
      "properties": {
        "name": {
          "description": "Name of the federated credential.",
          "type": "string",
          "minLength": 1
        },
        "issuer": {
          "description": "OIDC issuer URL.",
          "type": "string",
          "examples": [
            "https://token.actions.githubusercontent.com"
          ]
        },
        "subjectType": {
          "description": "The kind of GitHub OIDC subject claim to bind the trust to. The command resolves this to the immutable subject at runtime.",
          "type": "string",
          "enum": [
            "environment",
            "branch",
            "pull_request"
          ]
        },
        "audiences": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          }
        }
      }
    },
    "roleAssignment": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "role",
        "resourceGroupId"
      ],
      "properties": {
        "role": {
          "description": "Azure RBAC role name.",
          "type": "string",
          "minLength": 1
        },
        "resourceGroupId": {
          "description": "Id of the resource group (from the top-level 'resourceGroups' array) this role is scoped to. The command resolves this to the resource group's full Azure resource ID, which allows cross-environment role assignments without duplicating naming logic.",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "githubEnvironment": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "secrets",
        "variables"
      ],
      "properties": {
        "name": {
          "description": "Name of the GitHub environment.",
          "type": "string",
          "minLength": 1
        },
        "protectionRules": {
          "$ref": "#/definitions/protectionRules"
        },
        "secrets": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/keyValue"
          }
        },
        "variables": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/keyValue"
          }
        }
      }
    },
    "protectionRules": {
      "description": "GitHub environment protection rules.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "requiredReviewers": {
          "description": "GitHub usernames or team slugs required to approve deployments to this environment.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "waitTimerMinutes": {
          "description": "Minutes to wait before allowing deployments to proceed.",
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "keyValue": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "value"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "value": {
          "type": "string"
        }
      }
    }
  }
}
