name: platform-ci

on:
  pull_request:
    branches:
      - main
    paths:
      - 'config/global-config.jsonc'
      - 'config/platform-config.jsonc'
      - '.github/workflows/platform-*.yml'
      - '.github/actions/setup-platform-kite/**'
  workflow_dispatch:

concurrency:
  # Ensure only one CI workflow runs per branch at a time
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: Validate IaC
    uses: ./.github/workflows/platform-validate.yml
    permissions:
      contents: read

  plan:
    name: Preflight IaC platform configuration
    needs: [validate]
    uses: ./.github/workflows/platform-provision.yml
    permissions:
      contents: read
      id-token: write
    with:
      # Dedicated bootstrap environment for the platform pipeline itself: its identity has
      # subscription-level RBAC, so it can see and preview every environment's resource group,
      # not just one. The WhatIf run evaluates the complete platform configuration and does not
      # deploy changes.
      environment: platform
      what-if: true
    secrets: inherit
